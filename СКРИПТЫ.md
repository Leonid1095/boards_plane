# Скрипты PLGames Board

## 📁 Структура

Теперь всего **2 скрипта**:

```
.
├── install.sh    # Для сервера (полная установка)
└── dev.sh        # Для разработки (локально)
```

Все старые скрипты перемещены в `.old_scripts/`

---

## 🖥️ install.sh - Установка на сервер

### Назначение
Полная автоматическая установка PLGames на чистый сервер из GitHub.

### Использование

```bash
sudo bash install.sh
```

### Что делает

1. **Проверяет систему:**
   - Root права
   - Свободное место (минимум 15GB)

2. **Устанавливает Docker** (если не установлен):
   - Docker Engine
   - Docker Compose
   - Настраивает автозапуск

3. **Клонирует проект:**
   - Из GitHub: `https://github.com/Leonid1095/boards_plane.git`
   - В директорию: `/opt/plgames`

4. **Настраивает конфигурацию:**
   - Создаёт `.env` файл
   - Генерирует пароли БД
   - Определяет IP сервера

5. **Собирает образы:**
   - Использует исправленный `Dockerfile.plgames`
   - Логирует в `/tmp/plgames-build.log`

6. **Запускает сервисы:**
   - PostgreSQL
   - Redis
   - Backend
   - Frontend

7. **Проверяет готовность:**
   - Ждёт PostgreSQL
   - Ждёт Backend API
   - Показывает статус

### Режимы работы

При повторном запуске предлагает:

1. **Обновить** - `git pull` + пересборка
2. **Переустановить** - удалить всё и заново
3. **Использовать текущую** - без git pull
4. **Отмена**

### Пример вывода

```
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  ✓ PLGames Board установлен!
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

🌐 Frontend:  http://45.67.89.123:8080
🔧 Backend:   http://45.67.89.123:3010
📊 GraphQL:   http://45.67.89.123:3010/graphql

📁 Директория: /opt/plgames
📝 Конфигурация: /opt/plgames/.env
```

### Требования

- Ubuntu/Debian сервер
- Root доступ (sudo)
- 15+ GB свободного места
- Интернет соединение

---

## 💻 dev.sh - Разработка

### Назначение
Быстрая работа с проектом во время разработки (локально или на сервере).

### Использование

```bash
bash dev.sh
```

### Интерактивное меню

```
1) Быстрый запуск (пересборка + запуск)
2) Только пересборка
3) Только запуск
4) Остановка
5) Полная очистка и пересборка
6) Логи (live)
7) Статус
```

### Описание опций

#### 1. Быстрый запуск
```bash
docker compose down
docker compose build
docker compose up -d
```
Полный цикл: остановка → пересборка → запуск

#### 2. Только пересборка
```bash
docker compose build
```
Собирает образы без запуска

#### 3. Только запуск
```bash
docker compose up -d
```
Запускает существующие образы

#### 4. Остановка
```bash
docker compose down
```
Останавливает все контейнеры

#### 5. Полная очистка
```bash
docker compose down -v
docker system prune -f
docker compose build --no-cache
docker compose up -d
```
**Внимание:** Удаляет БД! Полная пересборка с нуля.

#### 6. Логи
```bash
docker compose logs -f
```
Просмотр логов в реальном времени (Ctrl+C для выхода)

#### 7. Статус
```bash
docker compose ps
curl http://localhost:3010/api/healthz
curl http://localhost:8080
```
Показывает статус контейнеров и проверяет доступность

### Пример использования

```bash
# Первый запуск
bash dev.sh
# Выбираете: 1 (Быстрый запуск)

# Посмотреть логи
bash dev.sh
# Выбираете: 6 (Логи)

# Пересобрать после изменений
bash dev.sh
# Выбираете: 1 (Быстрый запуск)

# Остановить
bash dev.sh
# Выбираете: 4 (Остановка)
```

### Требования

- Docker установлен
- Файл `.env` существует
- Проект клонирован локально

---

## 📊 Сравнение

| Параметр | install.sh | dev.sh |
|----------|------------|--------|
| **Где использовать** | Сервер | Локально |
| **Требует root** | Да (sudo) | Нет |
| **Устанавливает Docker** | Да | Нет |
| **Клонирует из GitHub** | Да | Нет |
| **Создаёт .env** | Да | Нет (использует существующий) |
| **Интерактивный** | Частично | Полностью |
| **Цель** | Первая установка | Разработка |

---

## 🔄 Типичные сценарии

### Сценарий 1: Первая установка на сервер

```bash
# На сервере (Ubuntu/Debian)
sudo bash install.sh

# Следуйте инструкциям
# Укажите домен/IP
# Укажите порты (или оставьте по умолчанию)
# Ждите завершения (~20 минут)

# Откройте в браузере
# http://your-server-ip:8080
```

### Сценарий 2: Обновление на сервере

```bash
# На сервере
cd /opt/plgames
sudo bash install.sh

# Выберите: 1) Обновить
# Скрипт сделает git pull и пересоберёт
```

### Сценарий 3: Разработка локально

```bash
# На вашем компьютере
git clone https://github.com/Leonid1095/boards_plane.git
cd boards_plane

# Создайте .env (или скопируйте)
cp .env.example .env

# Запустите
bash dev.sh
# Выберите: 1

# После изменений в коде
bash dev.sh
# Выберите: 1 (пересборка + запуск)

# Посмотреть логи
bash dev.sh
# Выберите: 6
```

### Сценарий 4: Отладка проблем

```bash
# Если что-то не работает

# 1. Посмотреть статус
bash dev.sh
# Выберите: 7

# 2. Посмотреть логи
bash dev.sh
# Выберите: 6

# 3. Полная пересборка
bash dev.sh
# Выберите: 5
# ВНИМАНИЕ: удалит БД!
```

---

## 🗑️ Старые скрипты (архив)

Перемещены в `.old_scripts/`:

- `install-fixed.sh` - старый установщик
- `quick-deploy.sh` - быстрый деплой
- `quick-start.sh` - wizard настройки
- `deploy_production.sh` - production деплой
- `ai-update.sh` - AI обновление
- `commit-changes.sh` - коммит изменений
- `deploy_fix.sh` - фикс деплоя
- `fix_frontend.sh` - фикс frontend
- `restart.sh` - перезапуск
- `setup.sh` - настройка

Можете удалить `.old_scripts/` если не нужны:

```bash
rm -rf .old_scripts/
```

---

## 🆘 Помощь

### install.sh не работает

**Проблема:** "Permission denied"
```bash
# Решение: запустите с sudo
sudo bash install.sh
```

**Проблема:** "Not enough space"
```bash
# Решение: очистите Docker
sudo docker system prune -af
sudo docker volume prune
```

**Проблема:** "git: command not found"
```bash
# Решение: установите git
sudo apt-get update
sudo apt-get install -y git
```

### dev.sh не работает

**Проблема:** ".env file not found"
```bash
# Решение: создайте .env
cp .env.example .env
# Отредактируйте при необходимости
nano .env
```

**Проблема:** "Docker not found"
```bash
# Решение: установите Docker
# https://docs.docker.com/engine/install/
```

**Проблема:** "Permission denied" (Docker socket)
```bash
# Решение: добавьте себя в группу docker
sudo usermod -aG docker $USER
# Перелогиньтесь или:
newgrp docker
```

---

## 📝 Логи и отладка

### Логи сборки

```bash
# install.sh сохраняет логи в:
/tmp/plgames-build.log

# Посмотреть:
cat /tmp/plgames-build.log

# Последние 50 строк:
tail -50 /tmp/plgames-build.log
```

### Логи контейнеров

```bash
# Все сервисы
docker compose logs -f

# Только backend
docker compose logs -f backend

# Последние 100 строк
docker compose logs --tail=100 backend
```

### Проверка состояния

```bash
# Статус контейнеров
docker compose ps

# Использование ресурсов
docker stats

# Проверка API
curl http://localhost:3010/api/healthz
```

---

## 🎯 Рекомендации

### Для production (на сервере)

1. Используйте `install.sh`
2. Настройте firewall
3. Используйте домен вместо IP
4. Настройте SSL (Nginx + Certbot)
5. Настройте автоматические бэкапы

### Для разработки (локально)

1. Используйте `dev.sh`
2. Часто смотрите логи (опция 6)
3. При проблемах - полная пересборка (опция 5)
4. Проверяйте .env перед запуском

---

**Дата создания:** 6 декабря 2024
**Версия:** 1.0
**Статус:** ✅ Готово к использованию
