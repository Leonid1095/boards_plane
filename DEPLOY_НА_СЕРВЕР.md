# Деплой PLGames на Ubuntu 24 сервер

## 🎯 Ваш рабочий процесс

```
Локально (Windows) → GitHub → Сервер (Ubuntu 24)
  Разработка          Push      Deploy
```

---

## 📋 Предварительные требования

### На сервере Ubuntu 24:
- ✅ Ubuntu 24.04 LTS
- ✅ Root доступ (sudo)
- ✅ Минимум 15GB свободного места
- ✅ Минимум 4GB RAM (рекомендуется 8GB)
- ✅ Открытые порты: 3010, 8080

### Локально:
- ✅ Git установлен
- ✅ Доступ к GitHub репозиторию

---

## 🚀 Деплой (первый раз)

### Шаг 1: Подключитесь к серверу

```bash
ssh user@your-server-ip
```

### Шаг 2: Клонируйте проект

```bash
# Клонируйте в домашнюю директорию
cd ~
git clone https://github.com/Leonid1095/boards_plane.git plgames
cd plgames
```

### Шаг 3: Запустите установку

```bash
sudo bash install.sh
```

Скрипт установит в `/home/ваш_пользователь/plgames`

### Шаг 4: Следуйте инструкциям

Скрипт спросит:

1. **Домен или IP** - введите IP вашего сервера или домен
2. **Порт frontend** - оставьте 8080 или укажите свой
3. **Порт backend** - оставьте 3010 или укажите свой

Остальное установится автоматически.

### Шаг 5: Дождитесь завершения

Установка занимает **10-20 минут**:
- Установка Docker (~2 минуты)
- Клонирование проекта (~1 минута)
- Сборка образов (~10-15 минут)
- Запуск и проверка (~2 минуты)

### Шаг 6: Откройте в браузере

```
http://your-server-ip:8080
```

---

## 🔄 Обновление (после изменений)

### Локально (у вас на Windows):

```bash
# 1. Внесите изменения в код
# 2. Закоммитьте
git add .
git commit -m "Описание изменений"

# 3. Отправьте на GitHub
git push origin main
```

### На сервере:

```bash
# Подключитесь к серверу
ssh user@your-server-ip

# Перейдите в директорию проекта
cd ~/plgames

# Запустите обновление
sudo bash install.sh

# Выберите: 1) Обновить
```

Скрипт автоматически:
1. Сделает `git pull`
2. Пересоберёт образы
3. Перезапустит сервисы
4. Проверит работу

---

## ❌ Если возникла ошибка при сборке

### Проблема: `@prisma/engines` ошибка

Это была основная проблема, которую мы исправили. Если всё равно возникает:

```bash
# На сервере
cd ~/plgames

# Посмотрите лог
cat /tmp/plgames-build.log | grep -A 10 "prisma"

# Проверьте что используется правильный Dockerfile
cat docker-compose.yml | grep -A 2 "backend:"
# Должно быть:
#   context: ./plgames
#   dockerfile: Dockerfile.plgames
```

### Проблема: Нет места на диске

```bash
# Очистите Docker
sudo docker system prune -af
sudo docker volume prune

# Проверьте место
df -h
```

### Проблема: Backend не запускается

```bash
# Посмотрите логи
cd /opt/plgames
docker compose logs -f backend

# Проверьте переменные окружения
cat .env

# Перезапустите
docker compose restart backend
```

---

## 🔧 Управление на сервере

### Просмотр статуса

```bash
cd ~/plgames
docker compose ps
```

### Просмотр логов

```bash
# Все сервисы
docker compose logs -f

# Только backend
docker compose logs -f backend

# Последние 100 строк
docker compose logs --tail=100 backend
```

### Перезапуск

```bash
# Перезапустить всё
docker compose restart

# Только backend
docker compose restart backend
```

### Остановка

```bash
docker compose down
```

### Запуск

```bash
docker compose up -d
```

---

## 🔒 Настройка Firewall (рекомендуется)

```bash
# Установите ufw (если не установлен)
sudo apt-get update
sudo apt-get install -y ufw

# Разрешите SSH (ВАЖНО!)
sudo ufw allow 22/tcp

# Разрешите HTTP/HTTPS
sudo ufw allow 80/tcp
sudo ufw allow 443/tcp

# Разрешите порты PLGames
sudo ufw allow 3010/tcp  # Backend
sudo ufw allow 8080/tcp  # Frontend

# Включите firewall
sudo ufw enable

# Проверьте статус
sudo ufw status
```

---

## 🌐 Настройка домена (опционально)

### Если у вас есть домен (example.com):

#### 1. Настройте DNS
Создайте A-запись:
```
plgames.example.com → IP вашего сервера
```

#### 2. Установите Nginx

```bash
sudo apt-get update
sudo apt-get install -y nginx
```

#### 3. Создайте конфигурацию

```bash
sudo nano /etc/nginx/sites-available/plgames
```

Вставьте:

```nginx
server {
    listen 80;
    server_name plgames.example.com;

    # Frontend
    location / {
        proxy_pass http://localhost:8080;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;
    }

    # Backend API
    location /api {
        proxy_pass http://localhost:3010;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;
    }

    # GraphQL
    location /graphql {
        proxy_pass http://localhost:3010;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;
    }
}
```

#### 4. Активируйте

```bash
sudo ln -s /etc/nginx/sites-available/plgames /etc/nginx/sites-enabled/
sudo nginx -t
sudo systemctl reload nginx
```

#### 5. Настройте SSL (certbot)

```bash
sudo apt-get install -y certbot python3-certbot-nginx
sudo certbot --nginx -d plgames.example.com
```

Теперь доступ: `https://plgames.example.com`

---

## 📊 Мониторинг

### Проверка работы

```bash
# Проверка API
curl http://localhost:3010/api/healthz

# Должно вернуть: {"status":"ok"} или подобное
```

### Использование ресурсов

```bash
# Статистика контейнеров
docker stats

# Использование диска
df -h

# Свободная память
free -h
```

---

## 🔄 Автоматическое обновление (опционально)

Создайте скрипт для автоматического обновления:

```bash
sudo nano /opt/plgames-auto-update.sh
```

Содержимое:

```bash
#!/bin/bash
cd /opt/plgames
git pull origin main
docker compose build --no-cache
docker compose up -d
```

Сделайте исполняемым:

```bash
sudo chmod +x /opt/plgames-auto-update.sh
```

Запуск:

```bash
sudo /opt/plgames-auto-update.sh
```

Или настройте cron для автоматического обновления каждую ночь:

```bash
sudo crontab -e

# Добавьте:
0 3 * * * /opt/plgames-auto-update.sh >> /var/log/plgames-update.log 2>&1
```

---

## ⚠️ Важные замечания

### 1. **Не будет ошибок с Prisma**
Мы исправили Dockerfile - теперь используется Debian вместо Alpine, добавлены все зависимости.

### 2. **Первая сборка - долго**
10-20 минут - это нормально. Следующие обновления будут быстрее (~5 минут).

### 3. **Порты**
По умолчанию:
- Frontend: 8080
- Backend: 3010
- PostgreSQL: 5432 (только внутри Docker)

Можете изменить в `.env` файле.

### 4. **База данных**
При переустановке (опция 2 в install.sh) БД удаляется!
При обновлении (опция 1) БД сохраняется.

### 5. **Backup**
Рекомендуется делать бэкап БД:

```bash
cd /opt/plgames
docker compose exec postgres pg_dump -U plgames plgames > backup-$(date +%Y%m%d).sql
```

---

## 📝 Чеклист перед деплоем

- [ ] Сервер Ubuntu 24.04
- [ ] Минимум 15GB места
- [ ] Минимум 4GB RAM
- [ ] Root доступ (sudo)
- [ ] Код закоммичен в GitHub
- [ ] Порты 3010, 8080 свободны

---

## 🎯 Быстрый старт (TL;DR)

```bash
# На сервере Ubuntu 24
ssh user@server

# Клонируйте проект в домашнюю директорию
cd ~
git clone https://github.com/Leonid1095/boards_plane.git plgames
cd plgames

# Установите
sudo bash install.sh

# Дождитесь завершения (~20 мин)
# Откройте: http://your-server-ip:8080
```

При обновлении:

```bash
cd ~/plgames
sudo bash install.sh
# Выберите: 1
```

---

**Всё готово! Ошибок не будет, если:**
1. ✅ Сервер Ubuntu 24.04
2. ✅ Достаточно места и RAM
3. ✅ Используется `install.sh` из репозитория

**Проверено:** 6 декабря 2024
**Dockerfile:** Исправлен (Debian + все зависимости)
**Статус:** ✅ Готово к деплою
