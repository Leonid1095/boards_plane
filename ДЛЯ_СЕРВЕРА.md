# Деплой на ваш сервер Ubuntu 24

## 📍 Ваша структура

**На сервере:** `/home/plg/plgames`

## 🗑️ Очистка /tmp (если нужно)

На сервере выполните:

```bash
sudo rm -rf /tmp/plgames /tmp/plgames-install
```

## 🚀 Первый деплой

### 1. Подключитесь к серверу

```bash
ssh plg@your-server-ip
```

### 2. Перейдите в директорию проекта

```bash
cd ~/plgames
```

### 3. Убедитесь что код актуальный

```bash
git status
git pull origin main
```

### 4. Запустите установку

```bash
sudo bash install.sh
```

Если это первый запуск - скрипт спросит:
- Домен/IP (введите IP сервера)
- Порты (оставьте по умолчанию)

Установка займёт **10-20 минут**.

### 5. После установки

Откройте в браузере:
```
http://your-server-ip:8080
```

---

## 🔄 Обновление (после изменений)

### Локально (на Windows):

```bash
# 1. Делаете изменения
# 2. Коммитите
git add .
git commit -m "Описание"

# 3. Пушите
git push origin main
```

### На сервере:

```bash
# 1. Подключаетесь
ssh plg@your-server-ip

# 2. Переходите в проект
cd ~/plgames

# 3. Обновляете
sudo bash install.sh

# 4. Выбираете: 1) Обновить
```

---

## 🛠️ Управление

### Статус

```bash
cd ~/plgames
docker compose ps
```

### Логи

```bash
# Все сервисы
docker compose logs -f

# Только backend
docker compose logs -f backend
```

### Перезапуск

```bash
docker compose restart
```

### Остановка

```bash
docker compose down
```

### Запуск

```bash
docker compose up -d
```

---

## ❌ Если ошибки

### Посмотреть лог сборки

```bash
cat /tmp/plgames-build.log
```

### Проверить Dockerfile

```bash
cat docker-compose.yml | grep -A 2 "backend:"
```

Должно быть:
```yaml
backend:
  build:
    context: ./plgames
    dockerfile: Dockerfile.plgames
```

### Полная пересборка

```bash
cd ~/plgames
docker compose down -v
docker system prune -f
docker compose build --no-cache
docker compose up -d
```

---

## ✅ Всё готово!

**Проект:** `/home/plg/plgames`
**Скрипт:** `sudo bash install.sh`
**Обновление:** Выбрать опцию 1

**Ошибок не будет!** Всё исправлено для Ubuntu 24.
